<%- include("adminheader") -%>

    <div class="w3-third w3-black">
        Teams
    </div>
    Lewis Esports

    <div class="w3-container">
        <h3>Manage teams</h3>

        <!--
        <form action="/teams" method="post">
            <p>First Name: <input class="w3-input w3-border w3-light-grey" type="text" name="firstName" maxlength="13" size="13" /></p>
            <p>Last Name: <input class="w3-input w3-border w3-light-grey" type="text" name="lastName" maxlength="30" size="30" /></p>
            <p>Type: <select class="w3-select w3-border" name="clothingType">
                <option value="camo shortsleeve">Camo short sleeve</option>
                <option value="hoodie">Hoodie</option>
            </select></p>
            <p>Color: <select class="w3-select w3-border" name="clothingColor">
                <option value="red">Red</option>
                <option value="black">Black</option>
            </select></p>
            <p>Size: <select class="w3-select w3-border" name="clothingSize">
               <option value="S">S</option>
                <option value="M">M</option>
                <option value="L">L</option>
               <option value="XL">XL</option>
              <option value="XXL">XXL</option>
            </select></p>

         <input class="top-margin w3-button w3-black" type="submit" name="submit" value="Add Order" />
        </form>
        -->

        <form action="/addteam" method="post">
            <p><b>Add Team</b></p>
            <p>Name: <input class="w3-input w3-border w3-light-grey" type="text" name="newTeam" maxlength="60" size="60" /> </p>
            <input class="w3-button w3-black w3-margin-bottom" type="submit" name="submit" value="Add team" />
        </form>

        <table class="w3-table-all">
            <th>Teams</th>
            <% for(var i=0; i<teams.length; i++) { %>
              <tr>
                <td><%= teams[i] %></td>
              </tr>
            <% } %>
          </table>

    </div>

    <script>
        function myFunction() {
          var x = document.getElementById("nav");
          if (x.className.indexOf("w3-show") == -1) {
            x.className += " w3-show";
          } else { 
            x.className = x.className.replace(" w3-show", "");
          }
        }
    </script>
</body>
</html>